{
  "name": "cryptopolio",
  "version": "1.0.0",
  "description": "CryptoPolio — a crypto portfolio management app (Next.js client + Express API).",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "concurrently -n server,client -c blue,green \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm run dev",
    "build": "cd client && npm install && npm run build",
    "install-dependencies": "cd client && npm install && cd ../server && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.0.0"
  }
}
