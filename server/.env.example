# Copy to `.env` and fill in. Required by the backend.

# MongoDB connection string. Leave blank for an auto in-memory dev DB.
# Production: use your MongoDB Atlas SRV string.
DATABASE_URI=

# Secret used to sign/verify JWT auth tokens. Use a long random value in prod.
JWT_SECRET=change_me

# Port the API listens on.
PORT=5000
